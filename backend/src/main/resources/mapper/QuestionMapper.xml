<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiqb.mapper.QuestionMapper">

    <resultMap id="BaseResultMap" type="com.aiqb.entity.Question">
        <id column="id" property="id"/>
        <result column="subject_id" property="subjectId"/>
        <result column="type" property="type"/>
        <result column="difficulty" property="difficulty"/>
        <result column="content" property="content"/>
        <result column="options" property="options"/>
        <result column="answer" property="answer"/>
        <result column="explanation" property="explanation"/>
        <result column="knowledge_point" property="knowledgePoint"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="randomPick" resultMap="BaseResultMap">
        SELECT * FROM questions
        <where>
            deleted = 0
            <if test="subjectId != null">AND subject_id = #{subjectId}</if>
            <if test="type != null and type != ''">AND type = #{type}</if>
            <if test="difficulty != null">AND difficulty = #{difficulty}</if>
            <if test="knowledgePoint != null and knowledgePoint != ''">
                AND knowledge_point = #{knowledgePoint}
            </if>
            <if test="excludeId != null">AND id != #{excludeId}</if>
        </where>
        ORDER BY RAND()
        LIMIT #{limit}
    </select>

</mapper>
